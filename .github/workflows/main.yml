on:
  push:
    branches:
      - master

jobs:
  build:
    runs on: ubuntu-latest

    steps:
      - uses: action/checkout@v3

      - name: "login to ducker hup"
        run: docker login --username ahmed2200 --passwoed dckr_pat_4YX_jUWWxuAlXAyjCM5D2x35NvU
